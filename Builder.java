import java.io.*;

public class Builder {

    public static void build(Item[] spears, Item[] helms, Item[] chests, Item[] arms, Item[] legs,
            Item[] accessories) {

        // Build[] list = new Build[758912];
        // int i = 0;

        // for (Item s : spears) {
        // for (Item h : helms) {
        // for (Item c : chests) {
        // for (Item a : arms) {
        // for (Item l : legs) {
        // for (Item y : accessories) {
        // list[i] = new Build(s, h, c, a, l, y);
        // i++;
        // }
        // }
        // }
        // }
        // }
        // }
        // // i = 0 ;
        // // Node n = list.first;
        // // while (n != null) {
        // // i++;
        // // total += n.b.componentsList.length;
        // // n = n.n;
        // // }

        // // File txt = new File("BuildList.txt");
        // try {
        // FileOutputStream out = new FileOutputStream("BuildList.txt", true);
        // ObjectOutputStream objOut = new ObjectOutputStream(out);
        // for (Build b : list) {
        // objOut.writeObject(b);
        // }
        // objOut.close();
        // } catch (FileNotFoundException e) {
        // e.printStackTrace();
        // System.out.println(e.getCause());
        // } catch (IOException e) {
        // e.printStackTrace();
        // System.out.println(e.getCause());
        // }
    }
}
